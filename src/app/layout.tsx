import type { Metadata } from "next";
import { Cormorant_Garamond, Outfit } from "next/font/google";
import "./globals.css";

const cormorant = Cormorant_Garamond({
  variable: "--font-cormorant",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
  style: ["normal", "italic"],
  display: "swap",
});

const outfit = Outfit({
  variable: "--font-outfit",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "HAVEN — Boutique Hotel · Serenity Bay",
  description:
    "A sanctuary designed for those who seek beauty in simplicity. Haven boutique hotel, Serenity Bay.",
  openGraph: {
    title: "HAVEN — Boutique Hotel",
    description:
      "A sanctuary designed for those who seek beauty in simplicity.",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${cormorant.variable} ${outfit.variable}`}>
      <body className="min-h-full antialiased">{children}</body>
    </html>
  );
}
